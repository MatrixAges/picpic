name: Deploy gh-pages
on:
  push:
    branches:
      - master
jobs:
  build-and-deploy:
    runs-on: ubuntu-20.04
    steps:
    - name: Checkout
      uses: actions/checkout@master

    - name: Deploy
      uses: peaceiris/actions-gh-pages@v3
      run: npm install && node build/index.js
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./